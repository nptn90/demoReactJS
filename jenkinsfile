node {
    stage('Build Image and push to docker hub') {
        echo('build Image')
        build job: 'BUILD'
    }

    stage('start images in app-server') {
        echo('deploy code')
        build job: 'DEPLOY'
    }
}